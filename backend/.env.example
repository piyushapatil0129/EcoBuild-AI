# EcoBuild AI Backend Environment Configuration
PORT=8000
HOST=0.0.0.0
ENV=development

# MongoDB Atlas URI (Leave empty to use automatic embedded local database)
MONGODB_URI=
MONGODB_DB_NAME=ecobuild_ai

# Security
JWT_SECRET=ecobuild-ai-super-secret-jwt-key-2026-production
